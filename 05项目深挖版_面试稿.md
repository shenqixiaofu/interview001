# 项目深挖版面试稿

这份不是保底稿，而是“面试官继续往下追时你怎么接”的版本。

先说结论：

- 现在的 [面试题_含答案.md](/Users/openclaw/work/writen_test/面试题_含答案.md) 够你讲顺项目，不够你扛深挖。
- 这份稿子的作用，不是让你全背，而是让你知道每个项目真正值钱的点在哪。
- 你真正要背的是：`1分钟介绍` + `1条核心链路` + `1个最难问题` + `3个继续追问`。

---

## 一、AI 工作台与企业知识库平台

### 1分钟介绍

这是一个企业级 AI 工作台，不是单纯的聊天页面。它的核心是两条链路：一条是 AI 对话链路，负责模型选择、会话管理、知识检索增强和流式返回；另一条是知识链路，负责文档上传、解析、切片、向量化和可检索化。除此之外，它还带有转写、文档转换、TTS 这类本地重任务，所以本质上是一个“AI 能力 + 文档流转 + 本地任务调度 + 中后台管理”结合的平台。

### 核心链路

用户提问后，前端会带上会话 ID、模型信息和挂载文档范围，通过 WebSocket 发起对话请求；后端拿到请求后，先加载会话上下文，再判断是否要走知识库检索。如果走检索，就先把问题向量化，再在指定文档范围里召回相关切片，拼接成最终 Prompt，交给模型生成答案。模型返回后，后端把增量内容流式推给前端，前端再持续渲染并在结束后持久化整轮消息。

### 最难问题

最难的问题不是聊天本身，而是本地模型对话、知识库检索、Whisper 转写、文档转换这些任务会共用一套机器资源。早期容易出现任务互相抢占、内存峰值高、响应变慢，严重时还会报 500。这个问题后来是通过统一算力调度去缓解的，也就是把任务纳入同一套资源池、租约和释放规则里，先保稳定，再谈性能。

### 继续追问

1. 为什么这里选 WebSocket 而不是普通 HTTP？
答：因为这里不只是单向流式输出，还涉及停止生成、状态同步、重连恢复这些双向事件，WebSocket 更合适。

2. 为什么知识库不能简单理解成“把文档扔给模型”？
答：因为整文塞进去会超上下文、噪音高、成本也高。RAG 的核心是按问题动态召回相关知识，而不是让模型盲读全文。

3. RAG 要怎么分离线端和在线端？
答：离线端负责文档采集、清洗、解析、切片、向量化、建索引和增量更新；在线端负责问题向量化、召回、重排、拼上下文和生成答案。

4. 这个项目为什么更像 AI 工作台，不算成熟 Agent 平台？
答：因为它现在的核心还是对话、知识库和本地任务接入，偏“能力工作台”；如果要更像 Agent，还得补工具调用、任务规划、工作流编排和评测体系。

5. 如果继续做，你下一步最想补什么？
答：我会优先补工具调用、会话记忆和评测体系，因为这三块决定它能不能从“能用”走到“长期稳定落地”。

### 容易被问穿的点

- 检索调优细节不是你的主卖点，别硬扛成自己主导设计。
- 你更适合把重点放在：对话链路、WebSocket、文档处理阶段划分、任务状态和调度价值。

---

## 二、趣行智旅 SaaS 系统

### 1分钟介绍

这是一个面向文旅行业的多租户、多业态 SaaS 系统，包含管理员后台、酒店端小程序和景区端小程序。它的难点不在“页面多”，而在于一套底座既要支撑多租户隔离，又要同时服务酒店和景区两种业态，还要承接在线客服、AI 对话、反馈、打卡、附近服务这些真实业务。

### 核心链路

这个项目最核心的不是某一个页面，而是“租户上下文 + 业务主体 + 多端页面”三者要统一。后台、小程序和后端都要在同一套数据语义下工作，不然就会出现页面展示、接口提交和底层存储对不上的问题。

### 最难问题

最典型的问题是 `ownerType / ownerId` 这类主体统一改造。因为早期很多模块天然是按酒店或景区单独长出来的，如果一直分两套字段和接口，后续能力一复用就会越来越碎。统一主体模型以后，很多能力才能真正复用，但改造过程中又不能一下子把旧接口和旧页面全部打散，所以才会强调“旧接口路径尽量不变，底层数据先切新模型”。

### 继续追问

1. 你怎么理解这个项目里的“租户”和“主体”？
答：租户更偏系统级上下文，解决隔离问题；主体更偏业务对象，解决同一套能力如何适配不同业态的问题。

2. 为什么 `ownerType / ownerId` 比 `hotelId` / `scenicId` 更适合长期演进？
答：因为它把“谁拥有这份业务数据”抽象出来了，后续扩展新业态时不需要每次再单独长一套模型。

3. 为什么“旧接口不动，底层切新模型”更稳？
答：因为前端已有页面、路由和调用方式可以先不改，风险更小，适合渐进式收口。

4. 这个项目里人工接管为什么难？
答：因为它不是单纯切消息来源，而是会话归属、展示状态、输入交互和服务端状态都要一起切。

5. 多端项目最容易出什么系统性问题？
答：最容易出的是字段口径不统一，看起来像小问题，实际会影响查询、编辑、回显、权限和运营配置。

### 容易被问穿的点

- 不要把它讲成“做了很多页面”。
- 你最值钱的答法是：多租户、多业态、主体统一、多端口径和实时客服状态同步。

---

## 三、NFC景区集章系统

### 1分钟介绍

这是一个景区互动营销系统，用户通过 NFC 标签触发页面，完成徽章收集、打卡和集章活动；后台则负责景区配置、徽章管理、标签管理、打卡记录和运营统计。它的价值不只是互动页面，而是把线下触达、用户参与和运营复盘串成了一个完整闭环。

### 核心链路

用户碰到 NFC 标签后，会跳到带参数的页面。前端读取标签参数后请求后端做校验，后端结合景区、标签、徽章和打卡记录给出当前解锁结果。前端根据结果点亮徽章、更新进度、播放音效，后台则持续记录和统计整条链路的数据。

### 最难问题

这个项目里最值钱的点不是动画，而是统计口径。比如为什么要区分原始碰一碰次数和有效碰一碰次数，为什么要做 30 秒去重，为什么要把“碰一碰 -> 打卡 -> 集齐”做成漏斗，这些其实决定了后台看到的是不是有运营价值的数据。

### 继续追问

1. 为什么要区分原始触达和有效触达？
答：原始触达更接近 PV，有效触达更接近真实参与人数，能过滤误触和短时间重复触发。

2. 为什么要做 30 秒去重？
答：是为了过滤同一设备短时间内的重复碰触，避免数据失真，同时又不把真正的再次参与全吞掉。

3. `deviceId` 在这个项目里为什么重要？
答：它是低成本识别“是否同一个设备”的核心依据，用于进度识别和统计去重。

4. 管理端漏斗的价值是什么？
答：能帮助运营判断问题是在入口、页面体验，还是玩法设计本身。

5. 如果某个标签触发很多但转化低，你怎么排查？
答：先看点位和误触，再看页面体验，最后看后端业务判断和记录逻辑。

### 容易被问穿的点

- 不要只讲“点亮徽章很好看”。
- 真正有分量的是：打卡识别、去重逻辑、统计漏斗和运营复盘价值。

---

## 四、现在怎么用这份稿

你别全背。

只背这三层：

1. 每个项目的 `1分钟介绍`
2. 每个项目的 `核心链路`
3. 每个项目的 `最难问题`

剩下的继续追问，只要你能说出关键词和逻辑顺序就够了。

---

## 五、最重要的提醒

- 现在那份基础题库不是废的，它是保底稿。
- 这份深挖稿是二层材料，专门防止你被继续追问时掉下去。
- 真正会让你挂掉的，不是“答得不够深”，而是“把不是自己主导的东西硬说成自己完全做过”。

你最稳的策略是：

- 保底题答顺
- 深挖题答实
- 边界讲清楚
