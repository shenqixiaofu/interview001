# 演示录屏指引

建议录制 60 至 90 秒，画面保持在桌面应用窗口内。

## 录制前准备

1. 在终端运行 `claude --version`，确认 Claude Code 已安装并登录。
2. 运行 `pnpm install` 和 `pnpm dev`。
3. 准备一个不含敏感信息的示例代码目录。
4. 清理演示时不需要展示的旧会话。

## 推荐脚本

1. 展示应用启动，顶部可见 Claude Code 版本。
2. 点击文件夹按钮，选择示例代码目录。
3. 在模型菜单中从 Sonnet 切换到 Haiku。
4. 输入“概括这个项目的技术栈和目录结构”，展示流式回复。
5. 点击新建按钮创建第二个会话，发送另一个简短问题。
6. 在左侧来回切换两个会话，证明历史内容独立保存。
7. 发起一个较长问题，点击停止按钮，展示停止状态。
8. 关闭并重新打开应用，展示会话仍然存在。

## 验收画面

- GUI 能直接发送消息并收到 Claude Code 回复。
- 模型选择器有可见变化。
- 至少两个会话可以互相切换。
- 流式状态、停止按钮和失败状态清晰可见。
- 录屏中不出现 API Key、私人目录内容或其他敏感信息。
