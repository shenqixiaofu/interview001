# 面试题（含参考答案）

## 自我介绍

面试官你好，我叫蓝剑豪，去年毕业于桂林航天工业学院计算机科学与技术本科，目前这个公司是全栈开发工程师。最近一年主要做了三类项目：`NFC景区集章系统`、`趣行智旅 SaaS 系统`、`AI 工作台与企业知识库平台`。

关于 AI 的使用经历，我是在做毕业设计的时候开始系统接触大模型的，当时主要用 Gemini 2.5 Pro 来辅助我梳理实现思路、整理论文结构和补充技术资料。进入工作之后，我开始把 AI 工具真正用到日常研发流程里，像 Gemini CLI、iflow 这类 agent 我更多拿来做需求整理、代码理解、问题排查和 debug 辅助。后面随着项目复杂度上来，我又开始用 Claude Code 和 Codex 作为主力辅助工具，去做方案梳理、联调定位和局部代码改造。

我做得比较多的工作是把一个需求从页面、接口、状态流转到联调交付串起来，不只是写单点功能。项目里我做过多端业务开发、实时聊天、知识库问答、NFC 打卡、文旅 SaaS 多主体配置这些场景，所以我现在更想继续往 AI 应用和业务型全栈这个方向发展。

## 一、简历核验

1. 你为什么把自己定位成“偏 Agent 应用方向”的全栈？
   答：因为我最近做的项目里，AI 工作台、知识库问答、流式对话、工具调用这一类内容比较多，不只是做页面，而是前端交互、后端链路和模型接入都要一起考虑，所以我会把自己定位成偏 Agent 应用方向的全栈。

2. 你现在的工作内容主要是什么？
   答：最近在弄一个叫私域变现应用的用户端h5和服务端h5，还有负责公司官网设计搭建

3. 你最能体现能力的一块是什么？
   答：我觉得是把一个功能从需求拆解到落地交付的完整闭环，尤其是多端项目里的状态同步、字段口径统一、接口联调和问题排查。

4. 你最近一次最棘手的问题是什么？
   答：是在 AI 工作台里处理本地模型对话、知识库检索、Whisper 转写和文档转换共用一套机器资源的问题。早期会出现任务互相抢占、响应变慢、内存峰值高，严重时还会报 500。后来是把聊天、RAG 和其他本地任务逐步接到统一算力调度里，通过资源池、任务租约、空闲释放和串行化控制先把稳定性保住。这个问题最难的地方在于，它不是一个单点 bug，而是运行时、调度和业务链路耦合在一起。

## 二、NFC景区集章系统

5. 先简单介绍一下这个项目。
   答：这是一个面向景区线下互动和数字文创场景的 NFC 集章系统，包含 Vue 3 管理后台、用户端 H5 和 FastAPI 后端，围绕景区配置、徽章收集、标签验证、打卡记录和统计分析形成完整闭环。

6. NFC 碰一碰的主链路是怎么跑的？
   答：用户访问写入 NFC 芯片的链接后，前端会根据 `unlock_id` 之类的参数识别标签，再请求后端做标签校验、打卡判断和进度返回；后端会结合景区、标签、徽章和打卡记录给出当前解锁结果，前端再做徽章点亮、状态刷新和交互反馈。

7. 为什么这个项目里要区分原始碰一碰次数和有效碰一碰次数？
   答：因为原始次数更接近 PV，能看标签被触发了多少次；有效次数更接近真实参与人数，能过滤误触和短时间重复触发。这样管理端做统计时，才能看出“标签是否被扫到”和“用户是否真的参与”是两回事。

8. 为什么要做 30 秒去重？
   答：这是为了避免同一个设备短时间连续触发把数据刷高。30 秒这个窗口足够覆盖一次正常的感应和页面跳转过程，既能防止误触，也不会把真正的重复参与完全抹掉。

9. `deviceId` 在这个系统里具体起什么作用？
   答：它一方面用于辅助识别同一个设备的打卡进度，避免同一人反复刷同一标签；另一方面也用于统计去重，比如参与用户数和有效碰一碰次数都和设备维度有关。

10. 管理端为什么要做“碰一碰 -> 打卡 -> 集齐”的漏斗？
   答：因为这个项目本质上不只是技术功能，还要看运营效果。漏斗可以帮助判断问题出在哪一层，比如碰一碰很多但打卡少，可能是页面体验或者链路问题；打卡多但集齐少，可能是玩法设计或难度问题。

11. 如果某个标签原始触发很多，但打卡转化很低，你会怎么排查？
   答：我会先看标签本身是不是放在高流量位置，或者是否存在误触；再看前端页面加载、跳转和交互是否顺；最后看后端校验和记录逻辑有没有把用户卡住。也就是把“入口触发”“页面体验”“业务判断”三层拆开查。

12. 这个项目里最能体现你全栈能力的一块是什么？
   答：是碰一碰统计和打卡链路这一块，因为它同时涉及前端参数识别、后端记录逻辑、去重统计、管理端数据展示和业务分析，不是只做一个接口或者一个页面。

## 三、趣行智旅 SaaS 系统

13. 先介绍一下这个项目。
   答：这是一个面向文旅行业的多租户、多业态 SaaS 系统，交付形态包括 Vben 管理后台、多租户端 uni-app x 小程序和 Java 后端服务，覆盖配置管理、内容运营、AI 客服、服务请求、反馈、打卡和附近服务等能力。

14. 你怎么理解这个项目里的“租户”和“主体”？
   答：租户更偏系统级上下文，决定当前操作的是哪一家的数据空间；主体更偏业务对象，比如酒店、景区这种具体归属对象。租户解决隔离问题，主体解决同一套业务如何适配不同业态的问题。

15. 为什么要做 `ownerType / ownerId` 这种改造？
   答：因为如果酒店和景区一直各走一套字段和接口，后续业务能力一扩展就会越来越碎。统一成 `ownerType / ownerId` 之后，可以把很多原本分散在酒店、景区两条线里的能力抽成同一套主体模型，后续扩新主体也更容易。

16. 为什么这个改造里会强调“旧接口路径尽量不变，底层数据来源切新模型”？
   答：因为这是风险更小的改法。前端页面、路由和已有调用方式可以先保持不动，后端先把底层数据来源切到新主体链路，等主链路稳定后再慢慢做前端字段映射和历史模块收口，这样不会一下子把所有端都打散。

17. 多端项目里，字段口径不统一最容易出什么问题？
   答：最直接的问题是前端展示和提交字段不一致，比如页面上还在用 `hotelId`，接口底层已经切成 `ownerId`；再往后就是状态流转出错，某些端能查到数据，某些端查不到，或者编辑后回显丢失。这个问题在多端项目里非常常见。

18. 你们在线客服 / AI 聊天里，人工接管这类场景为什么难？
   答：难点在于它不是单纯发消息，而是会话归属、消息展示、接管状态和前后端同步都要一起变。如果服务端状态没有统一好，就会出现前端看到已转人工，但服务端还按机器人会话处理的情况。

19. 你在这个项目里具体做了哪些模块？
   答：我参与了管理员后台开发与联调，负责过 AI 配置、打卡分类和多主体配置；也做过酒店端和景区端小程序，包括 AI 生活提案、反馈、打卡、附近服务、AR 导览和在线客服这些模块。

20. 小程序和管理后台怎么避免重复开发？
   答：我的做法一般是先统一字段模型和接口语义，把业务规则尽量放在服务端或者公共数据结构里；页面层再按端去做展示和交互差异，这样复用率更高，也更不容易联调打架。

21. 这个项目里你觉得最有代表性的技术问题是什么？
   答：我觉得是“多租户 + 多主体 + 多端并行迭代”带来的口径统一问题。因为这类问题表面上看像字段命名，实际上会影响查询、编辑、权限、页面回显和运营配置，是典型的系统性问题。

## 四、AI 工作台与企业知识库平台

22. 先介绍一下这个项目。
   答：这是一个企业 AI 工作台与知识库平台，包含聊天、知识库、文档中心、模型管理、会议转写、文档转换、TTS、Agent 等模块，提供 AI 对话、统一文档中心、知识检索增强问答和本地 AI 任务能力。

23. 知识库问答的完整链路怎么讲？
   答：先上传文档，再做解析和切片，然后向量化入库；提问时先做召回，把相关片段取出来，再拼到模型上下文里生成最终回答。

24. 为什么文档处理要拆成“上传、解析、切片、向量化”几个阶段？
   答：因为这几个阶段的耗时、失败点和资源占用都不一样。拆开以后更方便排错、重试和调度，也更适合接入队列或者统一任务系统，不会一个步骤失败把整条链路都卡死。

25. 你怎么理解 `LLM API`、工具调用、`Agent Loop`、`Prompt/Context Engineering` 这几个概念？
   答：我会把它们理解成一条从“模型接入”到“真正能办事”的链路。`LLM API` 解决的是怎么统一接模型，工具调用解决的是怎么让模型去查系统、调接口或执行动作，`Agent Loop` 解决的是模型怎么在“思考 - 调工具 - 再思考”之间循环，`Prompt/Context Engineering` 解决的是怎么把系统提示、历史消息、检索结果和业务状态组织好，让模型在合适的上下文里稳定输出。它们放在一起，才更像一个真正的 AI 应用，而不只是一个聊天窗口。

26. 这个项目里模型切换主要处理什么问题？
   答：主要是把不同模型供应商统一到一套调用方式里，包括请求参数对齐、流式返回格式处理、错误处理，以及前端怎么切换模型、保持会话状态和渲染一致性。

27. 流式对话前端怎么实现？
   答：前端会按增量内容持续更新同一条消息，保证消息顺序和会话 ID 一致，避免每次都整段重渲染；同时也要考虑中断、恢复和状态持久化，不然聊天体验会比较差。

28. 为什么本地模型对话、转写、文档转换会变成一个系统问题？
   答：因为它们都要吃本地机器资源，不只是 GPU 或 CPU 本身，还包括内存、模型加载时间和任务并发。如果不统一调度，就会出现一个任务把资源占满，其他链路全部抖动的情况。

29. 你刚才提到的统一算力调度，核心价值是什么？
   答：核心价值是把原本彼此抢资源的任务纳入同一套调度规则里，控制谁先执行、谁占多少资源、什么时候释放。这样做不一定让单次任务最快，但能明显提升整体稳定性和系统可维护性。

30. 这个项目里你做的内容更偏前端还是更偏全栈？
   答：还是偏全栈。前端我参与了聊天、模型切换、会话管理和消息渲染；后端和联调上，我也参与了文档上传、解析、切片、向量化，以及转写、文档转换这些本地任务链路。

31. 如果继续做这个平台，你下一步最想补什么？
   答：我会优先补三块：一是更稳定的文档处理和知识库链路，二是工具调用和会话记忆，三是权限控制和评测体系。因为这三块直接决定平台能不能从“能演示”走到“能长期用”。

## 五、通用技术

31. Vue 3 + TypeScript 项目里你怎么管理复杂状态？
   答：我会把页面局部状态、接口请求状态和全局共享状态拆开，页面内部尽量就近处理，跨组件共享的再放到 Pinia 或公共模块里，这样后期维护更清楚。

32. FastAPI 和 Spring Boot 的接口分层你怎么理解？
   答：核心思路差不多，控制层负责入参和出参，服务层负责业务编排，数据层负责查询和持久化。分层做好了，联调和排错都会轻松很多。

33. MySQL 和 Redis 在你的项目里分别怎么用？
   答：MySQL 负责业务数据持久化，Redis 更多承担缓存、会话、临时状态和高频读写支撑的角色。在 AI 工作台和 SaaS 里，这两个的分工都比较明确。
