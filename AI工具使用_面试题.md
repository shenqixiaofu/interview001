# AI 工具使用面试题

适合 AI 应用工程、Agent 应用、全栈开发和业务研发岗位面试使用。内容结合实际使用过的 `Gemini`、`Gemini CLI`、`iFlow`、`Claude Code` 和 `OpenAI Codex`，重点考察如何把 AI 工具用进真实研发流程，以及如何理解 Agent 的工具和 Skills 机制。

## 一、AI 工具使用基础

### 1. 你平时使用过哪些 AI 工具？分别用来做什么？

**答**：我接触过 Gemini、Gemini CLI、iFlow、Claude Code 和 OpenAI Codex。早期更多用 Gemini 辅助梳理毕业设计、论文结构和技术资料；工作中会用 CLI 类 Agent 做需求整理、代码理解、问题排查和 Debug；项目复杂之后，Claude Code 和 Codex 更常用于读取代码库、分析调用链、设计方案、修改局部代码和整理交付文档。

### 2. AI 对话工具、代码补全工具和 Agent 工具有什么区别？

**答**：代码补全工具主要根据局部代码预测后续内容，适合补函数、补模板；AI 对话工具更适合解释代码、讨论方案和生成文档；Agent 工具除了理解文本，还可以读取项目文件、执行命令、调用外部工具、修改代码，并根据执行结果继续推进。Agent 的能力更完整，但权限和风险也更高。

### 3. 你认为 AI 工具和普通搜索引擎有什么区别？

**答**：搜索引擎更适合查找原始资料和明确事实，AI 工具更适合结合上下文进行归纳、解释、生成和推理。实际工作中我不会把 AI 当成唯一事实来源，尤其是框架行为、命令参数和线上问题，仍然要回到官方文档、源码、日志和实际运行结果确认。

### 4. 你怎么判断一个问题适不适合交给 AI？

**答**：如果问题可以通过代码阅读、资料整理、模式归纳或多步分析提高效率，就适合交给 AI 辅助。涉及生产权限、敏感数据、不可逆操作或需要承担明确责任时，AI 只能提供建议，最终决定和执行必须由人确认。

### 5. 你使用 AI 工具时最看重什么？

**答**：我最看重三点：能不能理解当前项目上下文，输出是否能被验证，失败后能不能快速定位和修正。代码生成速度只是表面效率，如果产出的代码没有经过语法、类型、日志和业务核对，最后可能反而增加维护成本。

### 6. AI 工具在真实研发中最适合承担什么角色？

**答**：我会把 AI 当成能够快速阅读、归纳、推理和执行部分操作的研发助手。它可以帮助我扩大信息处理能力，但不能替代产品判断、架构决策、代码审查、权限控制和最终责任确认。

## 二、AGENTS.md 与项目上下文编写

### 7. `AGENTS.md` 是什么？

**答**：`AGENTS.md` 可以理解成写给 AI Agent 的项目级工作说明。它不是普通用户文档，而是告诉 Agent 在这个仓库里应该怎么读代码、怎么改代码、遵守哪些规范、哪些命令能跑、哪些操作需要确认。它的价值是把团队对 AI 协作的约束固化下来，让不同会话里的 Agent 都尽量按同一套规则工作。

### 8. `AGENTS.md` 通常应该写哪些内容？

**答**：通常会写项目背景、技术栈、目录职责、代码风格、命名规范、允许修改的范围、禁止操作、测试或检查方式、提交规范、文档维护要求和安全边界。核心不是写得长，而是让 Agent 明确“这个项目里什么能做、怎么做、做到什么程度算完成”。

### 9. `AGENTS.md` 和 README 有什么区别？

**答**：README 主要给人看，解决“项目是什么、怎么运行、怎么使用”；`AGENTS.md` 主要给 AI Agent 看，解决“进入项目后怎么工作、怎么修改、怎么验证”。README 更偏项目入口说明，`AGENTS.md` 更偏协作规则和行为约束。面试时可以说：我会把 README 当项目事实入口，把 `AGENTS.md` 当 Agent 操作手册。

### 10. `AGENTS.md` 和会话注入上下文有什么区别？

**答**：会话注入上下文是当前运行环境临时给 Agent 的系统级或任务级规则，优先级通常更高，比如语言要求、沙箱权限、审批规则和当前用户要求。`AGENTS.md` 是仓库里的长期项目规则，适合沉淀团队约定。简单说，注入上下文管当前会话，`AGENTS.md` 管这个仓库的长期协作方式。

### 11. 面试官问“除了 AGENTS.md 这种约束方式，还有其他的吗？”怎么答？

**答**：有，可以分成几类。第一是会话注入上下文，比如系统规则、开发者规则和用户当前要求；第二是仓库文档，比如 README、设计文档、接口文档和 `AGENTS.md`；第三是 Skills，把某类任务沉淀成可复用流程；第四是配置文件，比如沙箱、审批、模型和 MCP 配置；第五是工具权限，比如允许读文件、跑命令、改文件或访问外部服务。`AGENTS.md` 只是其中一种项目级约束方式。

### 12. `AGENTS.md` 怎么写才适合 AI 读取？

**答**：要写得直接、结构化、可执行。不要写太多抽象口号，而要明确步骤和边界，比如“先读哪些文件”“修改前要说明方案”“超过几个文件要拆任务”“禁止执行哪些危险命令”“修改后做哪些检查”。Agent 需要的是清晰指令，不是泛泛的价值观描述。

### 13. `AGENTS.md` 的推荐结构是什么？

**答**：可以按这些章节组织：

```md
# AGENTS.md

## 项目概览

## 目录说明

## 工作流程

## 代码规范

## 修改边界

## 验证要求

## 禁止操作

## 文档维护

## 面向 AI 的补充说明
```

这个结构能让 Agent 先理解项目，再知道应该怎么做事。

### 14. `AGENTS.md` 里“工作流程”应该怎么写？

**答**：应该写清楚 Agent 接到任务后的顺序。例如：先读取 README 和相关源码，使用搜索工具定位影响范围；需求不明确时先提问；修改前先说明方案；改动超过多个文件时拆成小任务；修改后检查 diff、语法和相关引用。这样可以减少 Agent 一上来就乱改代码的风险。

### 15. `AGENTS.md` 里“修改边界”应该怎么写？

**答**：要明确哪些文件可以改、哪些文件不能随便改，以及遇到用户已有改动时怎么处理。例如不要擅自重构无关模块，不要覆盖用户改动，不要执行破坏性 Git 命令，不要改生产配置，不要把兼容性逻辑随手塞进去。边界越具体，Agent 越不容易跑偏。

### 16. `AGENTS.md` 里“验证要求”应该怎么写？

**答**：应该结合项目实际写最小验证方式，比如只改 Markdown 就检查标题、链接和代码块；改 TypeScript 就做类型或语法检查；改接口就检查调用方和返回结构；改 UI 就截图或手动验证关键页面。验证要求不一定越多越好，关键是和改动风险匹配。

### 17. 一个简单的 `AGENTS.md` 示例怎么写？

**答**：

```md
# AGENTS.md

## 项目规则

- 先阅读 README、package.json 和任务相关源码。
- 修改前先说明方案，需求不清楚时先提问。
- 保持改动范围最小，不处理无关重构。
- 不覆盖用户已有改动，不执行破坏性 Git 命令。
- 修改 Markdown 后检查标题层级、链接和代码块。
- 修改代码后检查语法、导入和相关调用链。

## 项目重点

- electron/ 负责主进程、IPC、存储和 CLI 子进程。
- src/ 负责 React 渲染层、组件和样式。
- DESIGN.md 记录架构设计。
- AI_USAGE.md 记录 AI 使用和排障过程。
```

### 18. `AGENTS.md`、Skills 和工具之间是什么关系？

**答**：`AGENTS.md` 负责告诉 Agent 在当前仓库怎么工作，Skills 负责告诉 Agent 某一类任务应该按什么流程做，工具负责真正执行动作。比如 `AGENTS.md` 规定“改前先说明方案”，`AGENTS.md` 编写 Skill 规定“怎么沉淀仓库级规则”，Shell 或 Patch 工具负责读取文件、运行检查或修改文件。三者分别是仓库规则、任务流程和执行能力。

## 三、让 AI 理解项目

### 19. 你会如何让 AI 快速理解一个陌生代码库？

**答**：我会先让它读取项目入口、目录结构、依赖文件、启动命令和已有文档，再明确当前要解决的问题。对于具体功能，会继续追踪路由、组件、接口、服务层、数据模型和配置来源，最后让 AI 先复述它理解的调用链，我确认没有偏差后再进入修改阶段。

### 20. 为什么不能一开始就让 AI“直接改好这个项目”？

**答**：因为项目真正的约束通常分散在代码、配置、文档、运行环境和历史行为里。直接让 AI 修改，容易出现误解需求、改错边界、覆盖已有逻辑或引入与项目风格不一致的代码。更稳的方式是先收集上下文，再拆成可以验证的小问题。

### 21. 你通常会给 AI 提供哪些上下文？

**答**：主要包括需求目标、相关文件路径、现有实现、技术栈、运行命令、错误日志、输入输出示例和不能改变的约束。上下文不应当无差别堆满，而要围绕当前问题提供最相关的信息，否则模型容易被无关内容干扰。

### 22. 你怎么验证 AI 是否真的理解了项目？

**答**：我会让它先用自己的话说明当前架构、调用链、状态变化和修改影响范围，然后针对关键点追问。例如模型切换时，除了界面字段，还要确认真正传给运行时的模型值；流式对话时，还要确认消息 ID、会话 ID 和持久化时机是否一致。

## 四、Agent 的工具体系

### 23. 什么是 Agent 工具？

**答**：Agent 工具是模型可以调用的外部能力。模型负责理解目标、决定是否调用工具和组织下一步，工具负责执行读取文件、搜索代码、运行命令、访问网页或修改文件等具体动作。工具返回结果后，模型再根据结果继续判断。

### 24. Codex 这类 Agent 通常有哪些工具类别？

**答**：不同运行环境提供的工具名称可能不同，但面试时至少可以说出这五类：文件读取、文件搜索、Shell 命令、Patch 编辑、Web 查询。再展开一点，还可以补充浏览器操作、MCP 外部工具、子 Agent、计划管理、图片查看和可视化工具。

- 文件查看工具：读取源码、配置、日志和文档。
- 文件搜索工具：按文件名、关键词、正则或目录定位内容。
- Shell 工具：执行命令、查看 Git 状态、运行脚本和检查环境。
- 文件编辑工具：通过结构化补丁修改指定文件。
- Web 工具：搜索、打开和查阅官方文档或外部资料。
- 浏览器或计算机操作工具：访问网页、操作界面和截图验证。
- MCP 工具：连接外部系统、数据源、知识库或业务服务。
- 子 Agent 工具：把相互独立的分析任务拆分给其他 Agent。
- 计划与状态工具：拆解任务、跟踪进度和维护目标状态。
- 领域工具：例如图片生成、图像查看、数据分析或可视化工具。

所以如果面试官让说 5 个工具，不要回答 `/model`、`/compact`、`/clear`，而应该回答“读文件、搜文件、跑命令、打补丁、查文档”这类实际执行能力。

### 25. 文件读取和文件搜索工具分别解决什么问题？

**答**：文件读取用于理解已经定位到的具体文件，文件搜索用于在大量文件中快速找到相关路径或关键词。实际工作中通常先搜索范围，再读取关键文件，避免一开始把整个代码库无差别塞进上下文。

### 26. Shell 工具在 Agent 中有什么作用？

**答**：Shell 工具可以让 Agent 观察和操作真实开发环境，例如查看目录、读取 Git diff、执行语法检查、启动服务或复现问题。它的价值在于把“猜测”变成“通过命令获得证据”，但涉及删除、发布、权限和生产环境的命令必须严格控制。

### 27. 为什么结构化 Patch 比直接重写文件更适合改代码？

**答**：结构化 Patch 通常能明确表示新增、删除和修改的范围，更容易审查，也不容易覆盖用户已有的无关改动。对于局部需求，Patch 可以让修改保持最小，并通过 Git diff 进一步确认影响面。

### 28. Web 搜索工具在 Agent 任务中什么时候有价值？

**答**：当问题涉及会变化的版本、官方参数、最新 API、框架行为或明确需要引用来源时，Web 搜索很有价值。搜索结果不能只看摘要，应该打开原始文档确认上下文；技术问题优先使用官方文档、标准或源码等一手资料。

### 29. MCP 工具和普通函数工具有什么区别？

**答**：普通函数工具通常由当前应用直接定义和执行，适合少量明确的业务动作；MCP 更像一套标准化的外部工具和资源接入方式，可以让不同客户端或 Agent 以统一方式连接数据源和服务。它们都需要明确输入、输出、权限和错误处理。

### 30. 子 Agent 或多 Agent 适合解决什么问题？

**答**：适合把互相独立的任务并行处理，例如分别分析前端、后端和文档影响，再由主 Agent 汇总。任务之间存在强依赖或需要共享大量实时上下文时，不应该为了并行而强行拆分，否则协调成本可能超过收益。

### 31. Agent 使用工具的一般流程是什么？

**答**：通常是“理解目标、读取上下文、选择工具、执行工具、检查结果、继续规划、完成后验证”。工具调用不是一次性的函数执行，而是可能多轮循环；每一轮都要根据真实返回结果更新判断，不能假设工具一定成功。

### 32. Agent 工具设计最重要的原则是什么？

**答**：工具要职责单一、参数清晰、返回结构稳定、错误信息可判断，并且权限边界明确。读操作和写操作要区分，高风险写操作最好增加确认、幂等、审计和回滚机制。

### 33. Agent 为什么不能拥有无限权限？

**答**：因为模型可能误解任务，也可能根据错误上下文执行不合适的操作。无限权限会放大误删文件、泄露密钥、修改生产数据或执行危险命令的风险。实际设计中应该按目录、命令、服务和操作类型控制权限，必要时让人审批。

## 五、Skills 原理

### 34. Codex 中的 Skill 是什么？

**答**：Skill 可以理解成可复用的领域工作流说明包。它不是一句临时 Prompt，而是把某类任务的适用场景、执行步骤、工具使用方式、参考资料、脚本和输出要求沉淀下来，让 Agent 在遇到匹配任务时能够按统一流程工作。

### 35. Skill 是如何被 Agent 使用的？

**答**：通常先通过 Skill 的名称和描述判断当前任务是否匹配；匹配后读取入口说明，再按说明加载需要的参考资料、脚本、模板或资源。这个过程强调渐进式加载，只在任务需要时读取详细内容，避免所有 Skill 的全文都占用上下文。

### 36. Skill 和 Prompt 有什么区别？

**答**：Prompt 通常是一次对话里的任务指令，解决当前这一轮怎么回答；Skill 是可复用的任务规范，解决一类问题以后应该遵循什么流程。Prompt 更临时，Skill 更持久，也更容易在团队中共享和迭代。

### 37. Skill 和 Agent 有什么区别？

**答**：Agent 是能理解目标并调用工具完成任务的执行主体；Skill 是给 Agent 提供的领域知识和工作流程。可以理解为 Agent 负责执行，Skill 负责告诉它在特定领域如何执行得更稳定。

### 38. 一个本地 Skill 通常包含哪些文件？

**答**：最基本的是入口文件 `SKILL.md`，复杂 Skill 还可以包含：

```text
my-skill/
├── SKILL.md
├── scripts/
├── references/
├── assets/
└── templates/
```

`scripts/` 放可重复执行的脚本，`references/` 放详细资料，`assets/` 放图片或其他资源，`templates/` 放固定格式模板。不是每个 Skill 都必须包含所有目录。

### 39. `SKILL.md` 的基本结构是什么？

**答**：通常包含 YAML 元数据和正文说明：

```md
---
name: agents-md-writer
description: 编写或更新项目 AGENTS.md，适用于为 AI Agent 补充仓库级规则和协作约束
---

# AGENTS.md 编写 Skill

## 适用场景

## 前置读取

## 执行步骤

## 输出要求

## 验证方式

## 注意事项
```

元数据负责让 Agent 判断是否匹配，正文负责说明真正的执行流程。

### 40. Skill 的 `description` 应该怎么写？

**答**：应该明确两件事：什么时候使用，以及它解决什么问题。例如“编写或更新项目 AGENTS.md，适用于为 AI Agent 补充仓库级规则和协作约束”比“帮助处理文档”更容易被正确匹配。描述过于宽泛，容易误触发；过于狭窄，又可能漏掉应该使用的任务。

### 41. 编写 Skill 时为什么要先写适用场景和不适用场景？

**答**：适用场景能帮助 Agent 正确选择 Skill，不适用场景能减少误触发。例如 `AGENTS.md` 编写 Skill 适合更新仓库级 Agent 规则，但不应该被用于普通业务开发、接口联调或产品宣传文案。边界越清楚，执行结果越稳定。

### 42. Skill 的执行步骤应该写到什么程度？

**答**：要写到 Agent 可以照着执行的程度，例如先读取哪些文件、如何确认事实、允许修改哪些文件、输出前检查哪些内容。不能只写“分析项目并生成高质量文档”这种抽象目标，因为它没有告诉 Agent 具体怎么做。

### 43. Skill 什么时候应该使用 `references/`？

**答**：当详细资料较长、只在部分任务中需要，或者需要和主流程分开维护时，可以放到 `references/`。`SKILL.md` 只保留入口规则和引用路径，Agent 在确实需要时再读取对应资料。

### 44. Skill 什么时候应该使用 `scripts/`？

**答**：当某项操作重复、容易手工出错，或者需要结构化处理文件时，优先写脚本。例如自动检查 README 链接、统计目录、生成固定格式报告或解析日志。脚本应该有清晰输入输出，不能把复杂逻辑全部藏在一段无法审查的命令中。

### 45. Skill 中为什么要写验证方式？

**答**：因为 Skill 的目标不是“生成一段看起来合理的内容”，而是让任务真正闭环。验证方式应该说明如何检查文件是否存在、命令是否正确、链接是否有效、输出格式是否符合要求，以及哪些事实需要人工确认。

### 46. 如何编写一个 `AGENTS.md` 编写 Skill？

**答**：可以按以下流程设计：

1. 先读取当前会话注入上下文、README、依赖文件、启动脚本和核心目录。
2. 确认项目技术栈、目录职责、团队规则和验证方式。
3. 区分长期仓库规则和当前会话临时要求，避免把临时要求全部写进仓库。
4. 先列出 `AGENTS.md` 需要包含的章节和修改范围。
5. 写清工作流程、修改边界、禁止操作、验证要求和文档维护规则。
6. 检查规则是否具体、可执行、无歧义，并避免与现有文档冲突。
7. 输出变更摘要，并说明仍需人工确认的团队约定。

### 47. 一个简单的 `AGENTS.md` Skill 示例应该怎么写？

**答**：

```md
---
name: agents-md-writer
description: 编写或更新项目 AGENTS.md，适用于为 AI Agent 补充仓库级规则和协作约束
---

# AGENTS.md 编写说明

## 适用场景

- 新项目补充 AI Agent 工作规则。
- 现有 Agent 协作规则不清晰。
- 团队希望统一 AI 修改代码和验证代码的方式。

## 执行步骤

1. 读取会话注入上下文、README、依赖配置和核心目录。
2. 区分长期规则和当前任务要求。
3. 先列出 AGENTS.md 的章节和修改范围，再编辑文件。
4. 保持规则清晰、可执行、可验证。

## 输出要求

- 使用简洁中文说明。
- 包含工作流程、修改边界、验证要求和禁止操作。
- 不把当前会话的临时要求误写成长期仓库规则。

## 验证方式

- 检查 Markdown 标题层级。
- 检查规则是否具体、可执行、无互相冲突。
- 检查是否覆盖读文件、改文件、跑命令和文档维护。
```

### 48. 如何判断一个 Skill 写得好不好？

**答**：可以从五方面判断：触发条件是否准确，步骤是否足够具体，工具和文件范围是否明确，输出是否可检查，失败时是否知道如何处理。除此之外，还要用真实任务试跑，观察是否误触发、漏步骤或产生过多无关操作。

## 六、AI 辅助需求、编码与 Debug

### 49. 你如何让 AI 帮你拆解一个需求？

**答**：我会先给出目标、用户流程和约束，让 AI 分别拆出页面、接口、数据、状态、异常和验收标准。然后逐项确认哪些是必须做的，哪些是可选优化。对于 AI 工作台这类需求，还要单独拆模型接入、流式输出、会话状态、文档检索和任务调度。

### 50. AI 给出的技术方案你会直接采用吗？

**答**：不会直接采用。我会检查它是否符合当前代码结构、技术栈和实际业务约束，尤其关注数据来源、异常路径、并发行为、权限边界和部署条件。方案确认后再让 AI 做局部实现，避免一开始就生成大范围代码。

### 51. 你怎么写一个高质量的 Prompt？

**答**：我会明确角色、目标、已有上下文、约束、输出格式和判断标准。例如要求它先分析再修改，列出涉及文件和风险，不能擅自改动无关模块；如果是 Debug，则要求区分已确认事实、合理推断和仍需验证的假设。

### 52. 什么是 Context Engineering？你在工作中怎么用？

**答**：Context Engineering 可以理解为上下文工程，重点不是单独优化一句 Prompt，而是组织系统提示、项目规则、代码片段、历史对话、日志和工具结果。实际使用时，我会控制上下文范围，优先提供当前调用链和真实错误证据，并在多轮对话中持续校正模型对项目的理解。

### 53. 你会让 AI 一次生成完整功能吗？

**答**：小型、边界清晰的功能可以一次生成初稿，但复杂功能一般会拆成类型定义、接口、核心逻辑、界面状态和样式几个部分逐步完成。每一步都先检查 diff 和语法，再继续下一步，这样更容易发现问题，也能控制修改范围。

### 54. AI 生成的代码最容易出现什么问题？

**答**：常见问题包括虚构不存在的 API、忽略项目已有封装、字段命名不一致、异常路径缺失、状态更新顺序错误，以及把示例代码当成生产代码。解决方法不是继续让它重复生成，而是把真实文件、错误日志和明确的验收条件补给它。

### 55. 你遇到 Debug 问题时会怎么使用 AI？

**答**：我会先收集完整错误信息、复现步骤、相关日志和最近改动，再让 AI 根据证据提出多个可能原因。接着一次只验证一个关键假设，验证后把结果反馈给 AI，逐步缩小范围。这样可以避免让模型在没有证据的情况下不断猜测。

### 56. 结合当前项目，说一个 AI 帮助排障的例子。

**答**：项目早期界面只显示“Claude Code 退出，状态码 1”，定位价值很低。后来通过 AI 辅助补充调试日志、读取 stderr、检查模型列表、核对环境变量和本机配置，最终发现问题不只是认证变量名，还涉及用户级 Claude 配置污染。最后通过限制配置来源、隔离认证环境和补充日志，把问题定位链路建立起来。

### 57. 为什么“模型列表能查到”不代表“消息发送链路没问题”？

**答**：模型列表通常是应用直接发起的普通 HTTP 请求，而真正发送消息时可能还会经过 Claude Code CLI、子进程环境、认证配置和模型参数校验。两条链路经过的组件不同，所以必须分别验证，不能因为一个接口成功就推断整个系统可用。

### 58. 你如何避免 AI 修改过多文件？

**答**：开始前先明确允许修改的文件范围，要求 AI 先列出计划和影响面；修改后查看 `git diff`，确认没有无关改动。如果发现需要扩大范围，就先说明原因并重新确认，不让一个局部问题演变成没有边界的重构。

### 59. AI 写完代码后，你会做哪些验证？

**答**：至少会检查语法、类型、导入、调用参数、错误分支和实际 diff。涉及运行时行为时，还要用真实日志或最小流程验证；涉及数据写入、认证和外部服务时，则重点确认权限、密钥、超时、取消和失败后的状态是否正确。

## 七、Agent、Tool Calling 与安全

### 60. Agent 和 Workflow 有什么区别？什么时候先不用 Agent？

**答**：Workflow 是步骤固定、分支明确的流程；Agent 是模型根据目标和工具结果动态决定下一步。任务边界清晰、结果可预测时，优先使用普通代码或 Workflow，稳定性和可测试性通常更好。只有当任务需要根据上下文动态选择工具或路径时，才考虑 Agent。

### 61. Tool Calling 设计时要注意什么？

**答**：工具的名称、用途、参数和返回结构要清晰，参数要有明确类型和校验；错误结果要结构化，不能只返回一段模糊文本。涉及写操作时，需要权限校验、确认机制、幂等设计和审计记录，不能因为模型提出调用就直接执行高风险动作。

### 62. MCP 可以解决什么问题？

**答**：MCP 可以理解成模型接入外部工具、资源和上下文的一种标准化方式。它的价值是减少每个工具都单独设计私有协议的成本，方便后续接入知识库、工单、CRM 或内部数据源。但是否使用 MCP，仍然要看项目规模、工具数量和安全要求，不能为了追新技术强行引入。

### 63. 你如何避免把敏感信息发送给 AI？

**答**：不直接提供 API Key、密码、用户隐私、生产数据库内容和未脱敏日志。需要分析时先脱敏，只保留复现问题所需的字段和结构；如果必须处理内部资料，要遵守公司的数据分级、访问权限和工具使用规定。

### 64. AI 生成的代码有安全风险吗？

**答**：有，常见风险包括硬编码密钥、命令注入、SQL 注入、权限校验遗漏、危险命令参数和不安全的文件路径处理。AI 生成后必须结合项目的安全边界检查，尤其不能因为代码“看起来合理”就跳过权限和输入校验。

### 65. 什么时候不应该使用 AI？

**答**：当任务极其简单且人工直接完成更快时，不必为了使用 AI 增加沟通成本；当问题涉及未授权的敏感数据、不可逆生产操作或需要明确责任判断时，也不能让 AI 直接决定或执行。AI 是效率工具，不是责任转移工具。

### 66. 如果 AI 给出的答案和你的判断冲突，你会怎么办？

**答**：不会只因为 AI 说得完整就接受，也不会只凭直觉否定。先拆出冲突点，分别查代码、官方文档、日志或最小可复现实验；如果证据仍不足，就把不确定性保留下来，不把推测写成确定结论。

### 67. 如何衡量 AI 工具到底有没有提高效率？

**答**：不能只看生成代码行数，更应该看需求交付时间、问题定位时间、返工次数、缺陷数量和文档完整度。对于 AI 应用，还可以增加检索命中率、回答准确性、工具调用成功率、用户反馈和失败原因等指标。

### 68. 如果让你用 AI 完成一个新功能，你会怎么推进？

**答**：

1. 先确认需求目标、用户流程和验收标准。
2. 读取项目结构、相关模块、依赖和现有约束。
3. 让 AI 复述理解并给出影响文件和实现方案。
4. 将功能拆成小步骤，逐步修改并检查 diff。
5. 用语法检查、日志和最小业务流程验证结果。
6. 处理失败分支、权限和边界情况。
7. 最后把真实实现、排障过程和使用方式沉淀到 `AGENTS.md`、README 或项目文档中。

## 八、补充概念辨析

### 69. Codex 中的本地 Skill 和 OpenAI API 的 Skills 是一回事吗？

**答**：不应该直接混为一谈。Codex 本地 Skill 更偏 Agent 工作流和本地指令资源，用来指导代码库任务；OpenAI API 的 Skills 是平台侧可管理、可版本化和可通过 API 操作的资源。面试时应该先说明自己讨论的是哪一层，避免把本地工作流和 API 资源管理混在一起。

### 70. 你认为 AI 工具使用能力的核心是什么？

**答**：核心不是会写几句 Prompt，而是能把问题拆清楚、把上下文给准确、把工具选合适、把结果验证掉，并且知道什么时候该停止相信模型。真正有价值的是形成“需求分析、AI 协作、工具执行、人工判断、实际验证、结果沉淀”的完整闭环。

## 参考资料

- [OpenAI API Reference：Skills](https://developers.openai.com/api/reference/go/resources/skills)
- [OpenAI API Reference：创建模型响应与工具](https://developers.openai.com/api/reference/cli/resources/responses/methods/create)
