# 通用技术八股文（56题版）

适合 1 年左右工作经验背诵，配合《面试题_含答案.md》一起看。

这版重点往你的真实技术栈靠：`Vue 3`、`TypeScript`、`Pinia`、`Vue Router`、`uni-app x` 的通用前端思路，以及 `FastAPI`、`Spring Boot`、`MySQL`、`Redis`、`WebSocket`。

参考方向主要按牛客常见面经高频点来收敛，尤其是 `Vue 生命周期`、`组件通信`、`Pinia / Router`、`MySQL 索引 / MVCC`、`Redis 缓存问题`、`WebSocket 心跳重连` 这类高频问题。

---

## 0. 背诵顺序

- 先背 `1-20`：前端核心题，最贴近你简历。
- 再背 `21-40`：后端、数据库和缓存核心题。
- 最后背 `41-56`：网络、系统设计和 AI 应用加分题。

## 一、前端基础

### 1. Vue 3 的生命周期有哪些？最常用哪些？
**答**：常用的是 `setup`、`onBeforeMount`、`onMounted`、`onBeforeUpdate`、`onUpdated`、`onBeforeUnmount`、`onUnmounted`，配合 `activated / deactivated` 处理缓存组件。实际开发里最常用的是 `onMounted` 做初始化，`onUnmounted` 做清理。

### 2. 一般在哪个生命周期发请求？
**答**：通常在 `onMounted` 发请求比较直观，因为组件已经挂载完成；如果请求和 DOM 无关，也可以在 `setup` 里直接触发。核心不是死记周期，而是区分“依赖不依赖页面 DOM”。

### 3. `setup` 有什么特点？
**答**：`setup` 是组合式 API 的入口，组件实例创建前执行，拿不到 `this`，更适合把同一块业务逻辑组织在一起，方便复用和维护。

### 4. `ref` 和 `reactive` 有什么区别？
**答**：`ref` 适合基本类型，也能包对象，取值时要 `.value`；`reactive` 主要用于对象代理，直接访问属性。一般基本类型用 `ref`，对象状态用 `reactive`。

### 5. `computed`、`watch`、`watchEffect` 有什么区别？
**答**：`computed` 有缓存，适合派生状态；`watch` 明确监听某个源，适合副作用；`watchEffect` 会自动收集依赖，适合快速写响应式副作用，但依赖边界不如 `watch` 清晰。

### 6. `nextTick` 是做什么的？
**答**：它会等 Vue 把本轮 DOM 更新完成后再执行回调，常用于更新数据后立刻读取最新 DOM。

### 7. `v-if` 和 `v-show` 有什么区别？
**答**：`v-if` 是真正创建和销毁 DOM，切换成本高但初始开销低；`v-show` 是切换 `display`，适合高频显示隐藏。

### 8. `key` 在列表渲染里为什么重要？
**答**：`key` 帮助 Vue 识别节点身份，减少错误复用。列表渲染里最好用稳定且唯一的业务 id，不建议直接用索引。

### 9. `keep-alive` 有什么作用？
**答**：它可以缓存组件实例，避免切换时重复销毁和重建，常用于列表页和详情页切换、Tab 页面切换。

### 10. Vue 组件通信方式有哪些？
**答**：父传子用 `props`，子传父用 `emit`，跨层级可以用 `provide / inject`，跨页面或全局共享状态常用 `Pinia`。

### 11. `provide / inject` 适合什么场景？
**答**：适合祖先组件给后代组件传递上下文，比如表单容器、主题、业务上下文，不适合当成全局状态库乱用。

### 12. Pinia 和 Vuex 有什么区别？
**答**：Pinia 更轻、更贴合 Vue 3，写法更像组合式 API，类型推导也更友好；Vuex 更偏传统 mutation/commit 模式。现在 Vue 3 项目通常优先 Pinia。

### 13. Pinia 的 `state`、`getters`、`actions` 分别是什么？
**答**：`state` 存状态，`getters` 做派生计算，`actions` 写同步或异步业务逻辑。

### 14. Vue Router 常见的两种路由模式是什么？
**答**：`hash` 和 `history`。`hash` 兼容性好、部署简单；`history` 路径更美观，但服务端要做回退配置。

### 15. 路由守卫一般用来做什么？
**答**：常见用法是登录校验、权限控制、动态路由注入、埋点和页面切换前的数据处理。

### 16. 动态路由容易踩什么坑？
**答**：常见问题是重复添加、死循环跳转、路由守卫里拿不到状态仓库。解决思路是先判断是否已注入，再做一次定向跳转。

### 17. 什么是 scoped 样式隔离？有什么局限？
**答**：`scoped` 会给当前组件样式加作用域标记，避免样式污染；但对子组件深层节点可能需要 `:deep()` 才能改到。

### 18. TypeScript 里 `type` 和 `interface` 有什么区别？
**答**：`interface` 更适合对象结构和声明合并，`type` 更适合联合、交叉和工具类型组合。

### 19. TypeScript 泛型有什么作用？
**答**：让类型参数化，兼顾复用和类型安全，避免重复定义和 `any`。

### 20. 前端如何管理复杂页面状态？
**答**：把页面展示状态、接口请求状态和全局共享状态拆开；页面内部局部状态放组件里，跨组件共享状态放 Pinia，避免所有东西都堆到全局。

## 二、后端基础

### 21. 进程、线程、协程有什么区别？
**答**：进程有独立地址空间，线程共享进程内存，协程是用户态轻量调度单元，适合高并发 I/O 场景。
进程是资源分配的基本单位，线程是资源调度的基本单位，协程是用户态轻量级线程

### 22. FastAPI 的依赖注入怎么理解？
**答**：通过函数参数声明依赖，框架自动解析并注入，适合统一抽离数据库会话、登录用户、权限校验等逻辑。

### 23. FastAPI 中间件一般做什么？
**答**：常见做统一日志、traceId、鉴权、跨域、异常处理和请求耗时统计，执行过程类似洋葱模型。

### 24. FastAPI 为什么适合做 AI 应用后端？
**答**：它异步能力比较自然，类型注解和 Pydantic 校验很顺手，写 API、流式响应、WebSocket 和文件处理都比较轻量。

### 25. `async/await` 什么时候适合用？
**答**：适合 I/O 密集场景，比如请求外部服务、数据库访问、文件处理；CPU 密集任务不一定能靠 `async` 提速，往往还要配合进程池或任务调度。

### 26. FastAPI 里 WebSocket 一般怎么用？
**答**：服务端维护连接、接收消息、推送结果，常用于聊天、任务进度和流式输出；关键点是连接管理、异常关闭、心跳和重连。

### 27. Spring 里的 IoC / DI 是什么？
**答**：IoC 是控制反转，DI 是依赖注入。对象不自己 new 依赖，而是交给容器管理，方便解耦和测试。

### 28. Spring AOP 是什么？
**答**：把日志、事务、权限这类横切逻辑抽离出来，底层通常靠动态代理实现。

### 29. Spring Bean 生命周期有哪些阶段？
**答**：实例化、属性注入、回调、前置处理、初始化、后置处理、可用、销毁。

### 30. Spring 事务为什么重要？
**答**：它保证一组数据库操作要么都成功，要么都失败，避免只执行一半导致数据不一致。

### 31. 常见事务传播行为里最常用的是什么？
**答**：最常见的是 `REQUIRED`，表示如果当前有事务就加入，没有就新建；理解这个就能应对大部分业务场景。

## 三、数据库与缓存

### 32. MySQL 为什么常用 B+ 树索引？
**答**：树高更低、磁盘 I/O 更少，叶子节点有序，范围查询更友好。

### 33. 聚簇索引和二级索引有什么区别？
**答**：聚簇索引叶子节点存整行数据，二级索引叶子节点通常存主键值，查完整行时可能要回表。

### 34. 什么是覆盖索引？
**答**：查询字段全部都在索引里，不需要回表查整行，性能通常更好。

### 35. 什么是回表？
**答**：先通过二级索引查到主键，再回聚簇索引查整行数据，这个过程叫回表。

### 36. 联合索引最左匹配原则是什么？
**答**：联合索引会优先从最左列开始匹配，中间不能随意跳过，所以建索引时要按高频查询条件顺序设计。

### 37. 索引什么时候容易失效？
**答**：常见情况有对索引列做函数计算、隐式类型转换、`like '%xx'`、范围条件后继续匹配后续列等。

### 38. MySQL 的事务隔离级别有哪些？
**答**：读未提交、读已提交、可重复读、串行化。MySQL 默认通常是可重复读。

### 39. MVCC 是什么？
**答**：多版本并发控制，读操作尽量不加锁，通过版本可见性规则读取合适的数据版本。

### 40. 快照读和当前读有什么区别？
**答**：快照读读的是历史可见版本，通常不加锁；当前读读的是最新数据，可能会加锁，比如 `select ... for update`。

### 41. Redis 常用的数据结构有哪些？
**答**：最常用的是 String、Hash、List、Set、ZSet；实际业务里缓存对象常用 String/Hash，排行榜常用 ZSet。

### 42. Redis 持久化有哪几种？
**答**：RDB 做快照，AOF 记录写命令，也可以混合使用；前者恢复快，后者数据更完整。

### 43. 什么是缓存穿透、击穿、雪崩？
**答**：穿透是查不存在的数据，击穿是热点 key 失效，雪崩是大量 key 同时失效；常用空值缓存、布隆过滤器、互斥锁、随机过期时间和限流来处理。

### 44. Redis 分布式锁怎么实现？
**答**：常见做法是 `SET key value NX EX` 加锁，value 用唯一值，解锁时用 Lua 脚本先比对再删除，避免误删别人的锁。

### 45. 怎么理解数据库和缓存一致性问题？
**答**：严格强一致很贵，业务里更多是控制成最终一致。常见做法是先写数据库再删缓存，配合重试、延迟双删或消息队列。

## 四、网络、实时通信与系统设计

### 46. HTTP、HTTPS、WebSocket 的区别是什么？
**答**：HTTP 是请求响应协议，HTTPS 是加密版 HTTP，WebSocket 是基于 HTTP 握手升级后的双向通信协议。

### 47. HTTP 状态码中 2xx、3xx、4xx、5xx 分别代表什么？
**答**：`2xx` 表示请求成功，常见 `200` 成功、`201` 创建成功、`204` 成功但无内容；`3xx` 表示重定向或缓存协商，常见 `301/302` 重定向、`304` 缓存未修改；`4xx` 表示客户端请求有问题，常见 `400` 参数错误、`401` 未认证、`403` 无权限、`404` 不存在、`429` 请求过多；`5xx` 表示服务端或网关异常，常见 `500` 服务内部错误、`502` 网关收到上游异常响应、`503` 服务暂不可用、`504` 网关等待上游超时。

### 48. WebSocket 为什么要做心跳和重连？
**答**：因为长连接可能被网关、防火墙或网络抖动断开，心跳用于保活和探测断线，重连用于恢复通信。

### 49. 什么是幂等？
**答**：同一个请求执行一次和多次结果一致，常用请求 ID、唯一索引和状态机来保证。

### 50. 常见限流算法有哪些？
**答**：固定窗口、滑动窗口、漏桶、令牌桶；实际常用令牌桶，兼顾平滑和突发流量。

### 51. 消息队列常见使用场景有哪些？
**答**：异步解耦、削峰填谷、顺序消费和广播通知。

## 五、AI 应用加分题

### 52. Agent 和 Workflow 有什么区别？
**答**：Workflow 是固定流程，Agent 是模型自己决定下一步怎么做；流程固定优先用 Workflow。

### 53. 什么时候先不用 Agent？
**答**：任务明确、步骤固定、结果可预测时，先用 `Prompt + Workflow` 更稳也更好维护。

### 54. Tool Calling 设计要注意什么？
**答**：工具输入输出要结构化，错误行为要明确，别让模型猜结果。

### 55. 什么是 Context Engineering？
**答**：把系统提示、历史消息、业务状态、检索结果分层组织好，让模型拿到最有用的上下文。

### 56. RAG 的完整链路是什么？
**答**：RAG 一般分成离线端和在线端。离线端负责文档采集、清洗、解析、切片、向量化、建索引和增量更新；在线端负责接收问题、做查询改写或路由、向量召回、过滤重排、拼接上下文，再交给大模型生成最终回答，必要时还会返回引用来源。

### 57. Embedding、流式输出、Evals 分别做什么？
**答**：Embedding 负责向量化，流式输出负责边生成边展示，Evals 负责评测效果和回归检查。

### 58. 你怎么理解 LLM API、工具调用、Agent Loop、Prompt/Context Engineering？
**答**：我会把它们看成一条从“模型接入”到“真正能办事”的链路。`LLM API` 解决的是怎么统一接模型，工具调用解决的是怎么让模型去查系统、调接口或执行动作，`Agent Loop` 解决的是模型怎么在“思考 - 调工具 - 再思考”之间循环，`Prompt/Context Engineering` 解决的是怎么把系统提示、历史消息、检索结果和业务状态组织好，让模型在合适的上下文里稳定输出。它们放在一起，才更像一个真正的 AI 应用，而不只是一个聊天窗口。

### 59. LangChain、Agno、Chroma 分别做什么？
**答**：`LangChain` 更偏通用的 LLM 应用编排，适合把 Prompt、链路、工具和检索串起来；`Agno` 更偏轻量的 Agent 框架，适合做多工具调用和智能体流程；`Chroma` 是向量数据库，主要负责 embedding 存储和语义检索。放到业务里，它们通常是“编排层 + Agent 层 + 检索层”各司其职。

### 60. MCP 你怎么理解？
**答**：MCP 我会把它理解成模型和外部工具/资源之间的标准接入方式，更适合当成了解项看，不会把它写成我项目里的主技术栈。它的价值是统一接入工具、数据源和上下文，方便后续往工具调用和 Agent 平台演进。
