# 计算机网络面试题

适合全栈开发、前端开发和 AI 应用工程岗位面试使用。内容重点结合 `HTTP`、`HTTPS`、`WebSocket`、流式对话、前后端联调和线上排障场景。

## 一、网络基础

### 1. OSI 七层模型和 TCP/IP 模型分别是什么？

**答**：OSI 七层模型从下到上是物理层、数据链路层、网络层、传输层、会话层、表示层和应用层。实际开发里更常用 TCP/IP 模型，可以粗略分为网络接口层、网际层、传输层和应用层。我们平时接触的 HTTP、DNS 属于应用层，TCP、UDP 属于传输层，IP 属于网络层。

### 2. IP 地址和 MAC 地址有什么区别？

**答**：IP 地址用于标识网络中的逻辑位置，通常会随着网络环境变化；MAC 地址用于标识网卡的物理地址，主要在局域网内通信。简单理解，IP 更像目的地地址，MAC 更像当前局域网里下一跳设备的硬件地址。

### 3. TCP 和 UDP 有什么区别？

**答**：TCP 是面向连接的可靠传输协议，具备确认、重传、排序、流量控制和拥塞控制；UDP 无连接、不保证可靠性，但协议开销更小、传输延迟更低。普通接口、文件传输通常使用 TCP，实时音视频、部分实时通信场景更看重 UDP 的低延迟。

### 4. TCP 为什么是可靠的？

**答**：TCP 通过序列号保证数据有序，通过确认号确认数据已收到，通过超时重传或快速重传处理丢包，还会使用校验和发现数据损坏。另外，TCP 的滑动窗口、流量控制和拥塞控制可以避免接收方或网络被过快的数据压垮。

### 5. TCP 三次握手的过程是什么？

**答**：

1. 客户端发送 `SYN`，并携带自己的初始序列号。
2. 服务端回复 `SYN + ACK`，确认收到了客户端的序列号，同时发送自己的初始序列号。
3. 客户端再回复 `ACK`，确认收到了服务端的序列号。

完成后，双方都确认了对方的发送和接收能力，连接才正式建立。

### 6. TCP 为什么需要三次握手，两次不行吗？

**答**：两次握手只能让服务端确认客户端能发送、客户端确认服务端能发送，但无法让双方都确认自己的发送能力和对方的接收能力已经形成闭环。三次握手还可以避免历史连接中的失效请求突然建立连接，减少服务端资源被错误占用。

### 7. TCP 四次挥手的过程是什么？

**答**：TCP 是全双工通信，双方的发送方向需要分别关闭，所以通常需要四次：

1. 主动关闭方发送 `FIN`，表示自己不再发送数据。
2. 对方回复 `ACK`，表示已经收到关闭请求。
3. 对方处理完剩余数据后，再发送 `FIN`。
4. 主动关闭方回复 `ACK`，等待一段时间后关闭连接。

### 8. `TIME_WAIT` 是什么？为什么不能立即释放？

**答**：`TIME_WAIT` 是主动关闭方在发送最后一个 `ACK` 后保留的一段状态。它主要有两个作用：确保最后的确认报文能够让对方收到，以及让旧连接中的延迟报文在网络中自然消失，避免影响后续使用相同地址和端口建立的新连接。

### 9. 什么是流量控制和拥塞控制？

**答**：流量控制关注接收方的处理能力，通过接收窗口避免发送方发得太快；拥塞控制关注整个网络的承载能力，通过慢启动、拥塞避免、快速重传等机制避免网络拥塞。前者是端到端问题，后者是网络整体问题。

### 10. TCP 丢包后一般怎么处理？

**答**：如果发送方在超时时间内没有收到确认，会触发超时重传；如果连续收到多个重复确认，也可能触发快速重传。重传次数和超时时间通常还会影响接口层的超时设计，所以应用层不能无限等待。

## 二、HTTP 与 HTTPS

### 11. HTTP 请求的基本组成是什么？

**答**：一个 HTTP 请求通常包含请求行、请求头、空行和请求体。请求行包括方法、路径和协议版本；请求头描述认证、内容类型、缓存、客户端信息等；请求体用于传递表单、JSON 或文件数据。

### 12. 在浏览器输入一个 URL 后发生了什么？

**答**：大致会经历 URL 解析、DNS 域名解析、建立 TCP 连接、如果是 HTTPS 则进行 TLS 握手、发送 HTTP 请求、服务端处理并返回响应、浏览器解析 HTML 和资源，最后渲染页面。实际访问中还可能涉及代理、缓存、CDN、负载均衡和连接复用。

### 13. DNS 的作用是什么？

**答**：DNS 负责把域名解析成 IP 地址，让用户可以通过容易记忆的域名访问服务。解析过程通常会先查浏览器、操作系统和本地 DNS 缓存，缓存未命中后再向 DNS 服务器查询。

### 14. HTTP 常见请求方法有哪些？

**答**：常见的有 `GET`、`POST`、`PUT`、`PATCH` 和 `DELETE`。`GET` 通常用于查询，`POST` 用于创建或提交动作，`PUT` 更偏完整更新，`PATCH` 更偏局部更新，`DELETE` 用于删除。具体语义还要结合接口设计，不能只看方法名。

### 15. GET 和 POST 有什么区别？

**答**：GET 一般用于获取资源，参数通常放在 URL 中，适合缓存和分享；POST 通常用于提交数据，参数可以放在请求体中。两者的安全性不能只靠“参数放在哪里”判断，真正的安全性还要看 HTTPS、权限校验、输入校验和服务端处理。

### 16. 什么是幂等？哪些 HTTP 方法通常具有幂等性？

**答**：幂等是指同一个请求执行一次和执行多次，对服务端最终状态的影响一致。通常 `GET`、`PUT`、`DELETE` 具有幂等语义，但是否真正幂等还取决于接口实现；`POST` 默认不保证幂等，如果创建订单、提交任务等接口可能重复调用，就需要请求唯一 ID 或业务唯一约束。

### 17. HTTP 状态码中的 2xx、3xx、4xx、5xx 分别表示什么？

**答**：

- `2xx`：请求成功，例如 `200`、`201`、`204`。
- `3xx`：重定向或缓存协商，例如 `301`、`302`、`304`。
- `4xx`：客户端请求有问题，例如 `400`、`401`、`403`、`404`、`429`。
- `5xx`：服务端或网关处理异常，例如 `500`、`502`、`503`、`504`。

### 18. HTTP 长连接是什么？

**答**：HTTP 长连接通常指在多个请求之间复用同一条 TCP 连接，减少重复建立和关闭连接的成本。HTTP/1.1 默认支持连接复用，但它和 WebSocket 的长连接不是一回事，WebSocket 是升级后形成的独立双向通信协议。

### 19. HTTP/1.1 和 HTTP/2 有什么主要区别？

**答**：HTTP/1.1 主要通过持久连接减少握手，但多个请求可能受到队头阻塞影响；HTTP/2 使用二进制分帧、多路复用、头部压缩和服务器推送等能力，在同一连接上并发传输多个请求。实际是否提升明显，还取决于网络环境、服务端和客户端支持情况。

### 20. HTTPS 比 HTTP 多做了什么？

**答**：HTTPS 在 HTTP 和 TCP 之间增加了 TLS 层，主要提供加密传输、身份认证和数据完整性保护。它可以防止传输内容被轻易窃听或篡改，但不能替代应用层的登录鉴权、权限控制和输入校验。

### 21. TLS 握手主要解决什么问题？

**答**：TLS 握手主要用于协商加密算法、验证服务端证书、协商会话密钥。握手完成后，双方使用协商出的对称密钥加密业务数据，因为对称加密在大量数据传输时效率更高。

### 22. Cookie、Session 和 Token 有什么区别？

**答**：Cookie 是浏览器保存和自动携带的一种数据载体；Session 通常是服务端保存会话状态，客户端只保存 Session 标识；Token 通常由客户端保存并在请求中主动携带，服务端根据 Token 判断身份。实际项目中可以组合使用，关键是明确过期、撤销、刷新和安全策略。

### 23. 什么是跨域？为什么浏览器会限制跨域请求？

**答**：当页面的协议、域名或端口不同，就可能构成跨域。浏览器限制跨域主要是为了防止恶意页面在用户不知情的情况下读取其他站点的数据。服务端可以通过 CORS 响应头明确允许哪些来源、方法和请求头。

### 24. 什么是 CORS 预检请求？

**答**：对于非简单请求，浏览器会先发送 `OPTIONS` 请求询问服务端是否允许当前跨域操作。服务端需要返回允许的来源、方法和请求头；预检通过后，浏览器才会发送真正的业务请求。开发中遇到“前端代码没报接口错误但浏览器拦截”时，要重点看预检响应。

### 25. HTTP 缓存常见的控制方式有哪些？

**答**：常见有强缓存和协商缓存。强缓存通过 `Cache-Control`、`Expires` 判断是否直接使用本地缓存；协商缓存通过 `ETag / If-None-Match` 或 `Last-Modified / If-Modified-Since` 向服务端确认资源是否变化。接口缓存还要结合数据实时性和权限范围设计。

## 三、实时通信与流式场景

### 26. WebSocket 的建立过程是什么？

**答**：WebSocket 首先通过 HTTP 请求发起升级，客户端在请求头中带上 `Upgrade: websocket` 等信息。服务端同意后返回 `101 Switching Protocols`，之后双方就可以在同一条连接上进行双向消息通信。

### 27. WebSocket 和 HTTP 有什么区别？

**答**：HTTP 主要是客户端请求、服务端响应；WebSocket 在握手完成后可以让客户端和服务端主动发送消息。普通查询、分页和表单提交适合 HTTP，实时聊天、客服消息、任务进度和状态同步更适合 WebSocket。

### 28. WebSocket 和 SSE 怎么选择？

**答**：SSE 是服务端向客户端的单向推送，适合简单通知或只展示模型增量文本；WebSocket 支持双向通信，适合聊天、停止生成、心跳、重连恢复和状态同步。选择时看业务是否需要客户端主动向服务端发送实时事件。

### 29. WebSocket 为什么需要心跳？

**答**：长连接可能被网关、防火墙或网络抖动断开，而客户端不一定能马上感知。心跳可以定期确认连接还活着，并及时发现失效连接。心跳间隔要结合网关超时配置和业务实时性设置，不能无限频繁发送。

### 30. WebSocket 断线重连要注意什么？

**答**：需要维护连接状态，限制重连次数，并使用递增等待时间避免网络异常时瞬间产生大量连接。重连成功后还要恢复会话 ID、用户身份、页面上下文和未完成请求状态，同时用会话 ID 或请求 ID 校验消息，避免旧连接消息误渲染到新会话。

### 31. AI 流式对话从模型到前端大致怎么传输？

**答**：后端先调用模型的流式接口，持续读取增量 token 或文本片段，再通过 SSE 或 WebSocket 推给前端。前端根据会话 ID 和生成请求 ID，把增量内容追加到同一条助手消息中；生成结束后再保存最终消息和完成状态。

### 32. 流式对话中用户点击“停止生成”怎么处理？

**答**：前端需要记录当前生成请求的标识，点击停止后向后端发送取消事件。后端找到对应任务并取消下游模型读取，同时向前端发送最终状态。前后端都要校验 generation ID，避免取消后仍有旧请求的延迟片段继续写入当前消息。

## 四、网络排障与工程实践

### 33. 接口请求很慢，你会怎么排查？

**答**：先区分是 DNS、建连、TLS、服务端处理还是响应传输慢。可以在浏览器 Network 面板或命令行中观察 DNS、连接、等待和下载时间，再结合服务端日志、数据库查询耗时、外部服务耗时和网关日志定位。不要一看到页面慢就直接判断是前端问题。

### 34. 接口返回 502、503、504，分别应该怎么理解？

**答**：它们通常和网关或上游服务有关。`502` 常见于网关收到异常上游响应，`503` 表示服务暂不可用或过载，`504` 表示网关等待上游超时。排查时要把网关、应用服务、数据库和外部依赖串起来看，不能只重试接口。

### 35. 网络请求为什么不能无限重试？

**答**：无限重试会放大故障，形成请求风暴，还可能造成重复写入。重试应该设置次数上限、退避时间和明确的可重试条件；对于写操作，还要先保证幂等，否则重试可能重复创建订单、任务或消息。

### 36. 前端如何设计接口超时、取消和错误提示？

**答**：请求要有明确的超时时间，超时后释放资源并给用户可理解的状态提示。对于搜索、流式生成和页面切换等场景，要支持取消旧请求，避免旧响应覆盖新状态。错误提示要区分网络失败、权限失败、参数错误和服务端异常，方便用户处理和开发排障。

### 37. 上传大文件时为什么要考虑分片或流式传输？

**答**：一次性把大文件全部读进内存，容易造成内存峰值和请求超时。分片上传可以降低单次传输失败的成本，也便于断点续传；流式传输则可以边读取边处理。文档中心、音视频转写和文档转换场景尤其需要关注文件大小、超时和资源占用。

### 38. 结合你的项目，说说网络知识在哪里真正用到了？

**答**：AI 工作台里的模型调用、WebSocket 流式对话、停止生成、心跳和重连都涉及网络通信；SaaS 项目里的实时客服也依赖长连接和状态同步；NFC 项目虽然入口是标签链接，但后续仍然是浏览器请求、参数传递、接口校验和统计记录。遇到问题时，我会把链路拆成客户端、网关、应用服务、数据库和外部服务逐层排查。

