# AI 使用说明

## 使用的 AI 与工具

### OpenAI Codex（GPT-5）

用途：

- 从题图提取大题需求和交付要求。
- 设计 Electron、React 与 Claude Code CLI 的进程架构。
- 生成 TypeScript、React、CSS 和项目配置。
- 编写架构说明、运行说明和演示步骤。
- 检查跨进程类型、流式事件生命周期与基础语法问题。

### UI/UX Pro Max Skill

用途：

- 查询开发者工具的配色、字体和交互建议。
- 查询键盘导航、焦点状态、加载反馈、响应式布局和 React 类型约束。
- 形成交付前的视觉与可访问性检查清单。

### 本地工具

- 图片查看工具：读取笔试题图。
- 文件检索工具：盘点目录和核对交付文件。
- 补丁编辑工具：以可审查的增量方式创建代码和文档。
- TypeScript：执行语法和类型级检查。

## AI 产出中发现的问题

### 设计推荐与产品类型不完全匹配

UI/UX 查询结果同时给出了“单栏营销页”和“高饱和块状设计”，这与桌面开发工具的高频工作场景不符。

人工修正：保留其开发工具配色、等宽字体、可访问性和状态反馈建议，放弃营销页结构、大字号宣传标题和高饱和装饰，改为紧凑的会话栏加工作区。

### 流式内容不适合逐 token 持久化

初始方案可以在每个增量到达时写入 JSON，但这会造成频繁磁盘写入。

人工修正：流式阶段只更新渲染进程内存，正常结束、停止或失败时再统一写盘。

### 权限处理范围需要收敛

为了让 Claude Code 无人值守执行，可以传入跳过权限检查的参数，但桌面 GUI 没有展示权限影响范围。

人工修正：不加入自动批准或跳过权限的参数。应用只封装基础聊天，由 Claude Code 保持自身安全边界。

## 人工检查与修正范围

- 检查 preload 是否只暴露白名单方法，渲染进程是否关闭 Node.js 集成。
- 检查模型切换和 `session_id --resume` 是否都归属具体会话。
- 检查停止生成后消息是否进入错误/未完成状态，而不是永久保持 loading。
- 检查所有按钮是否具有名称、焦点状态、禁用状态和稳定尺寸。
- 检查长标题、长路径、Markdown 代码块和窄窗口是否溢出。
- 检查代码注释与文档是否使用 UTF-8 中文。

Claude Code CLI 是本应用运行时依赖，不是本次代码生成所使用的 AI；只有在用户实际发送消息时它才参与工作。
